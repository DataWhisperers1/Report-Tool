

# مكتبة Tkinter لإنشاء واجهة المستخدم الرسومية
tkinter

# مكتبة JSON للتعامل مع البيانات بتنسيق JSON
# (هذه المكتبة مدمجة في بايثون، لذا لا حاجة لتثبيتها بشكل منفصل)

# مكتبة pandas لتحليل البيانات وتصدير التقارير
pandas

# مكتبة smtplib لإرسال البريد الإلكتروني
# (مكتبة مدمجة في بايثون، لا تحتاج إلى تثبيت)

# مكتبة requests لتحميل والتحقق من صحة البروكسيات
requests

# مكتبة openpyxl لتصدير البيانات إلى تنسيق Excel (إذا كان ذلك مطلوبًا)
openpyxl

# مكتبة matplotlib لإنشاء الرسوم البيانية (إذا كنت بحاجة إلى تحليل بصري)
matplotlib

# مكتبة PyQt5 (إذا كنت تخطط لاستخدام واجهة مستخدم بديلة)
PyQt5